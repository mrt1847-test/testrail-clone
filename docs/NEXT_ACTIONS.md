# Next Actions

Updated: 2026-09-23. 실행 순서는 이 문서만 관리한다. 현재 worktree에만 반영됨.

## Current

**UI-066 — 작성·편집 이탈 시 공통 초안 보호.** 다음: UI-067.

2026-09-23 확인한 원본 상태 기준(UI-065 완료). 구현 전 진행 중 변경과 현재 작업 증거만 확인한다. 원본의 후속 진행을 발견하면 증거에 따라 갱신하며 완료를 추정하지 않는다.

범위: Close/Cancel/행·섹션·내비게이션·뒤로가기에서 Keep editing은 초안 유지, Discard는 한 번 이동, 저장 중 대상 고정. Text/Steps·실키·IME·1280/390·불필요 경고 없음을 검증한다.

UI 작업의 상세 원장: 원본 `C:/Users/cease/OneDrive/문서/GitHub/testrail-clone/docs/USABILITY_REALIGNMENT_2026-09-18.md`의 **현재 ID 항목만**. 이 worktree의 기존 원장은 오래된 상태이므로 완료 여부 판정에 사용하지 않는다.

## Next

| 순서 | ID | 목표 |
| --- | --- | --- |
| 1 | UI-067 | 본문/Steps 저장 후 화면 일치 |
| 2 | UI-068 | 케이스 작성 부분 실패·재시도 |
| 3 | PX-01 | Cases 계층·범위·추가 행동(UI-069 포함) |
| 4 | PX-02 | 작성·읽기·편집 페이지 구성(UI-070 포함) |
| 5 | UI-072 | 저장/취소 후 목록 문맥 복원 |
| 6 | UI-071 | 신규 첨부 staging·연결·복구 |
| 7 | PX-03 | Run 전체 구성·단일/일괄 대상 |
| 8 | PX-04 | 결과창 입력·부분 성공 흐름 |
| 9 | PX-05 | 나머지 페이지 공식 비교·후속 편성 |
| 10 | PX-05 후속 구현 | 근거로 확정한 수정만 수행 |
| 11 | PX-06 | 페이지 간 전체 여정 검증 |
| 12 | UI-021 | 통합 수용(E01 실제 bytes/E03 독립 관찰 포함) |

PX 범위/체크 원장: [실행 계획](./superpowers/plans/2026-09-23-testrail-page-ux.md). UI-069/070은 PX-01/02와 중복 실행하지 않으며 기존 수용 기준도 충족해야 완료한다.

## 매 실행의 최소 입력과 종료

1. 이 짧은 큐 + 현재 ID 항목 + 필요한 공통 제약만 읽는다. 계획 전체·연결 보고서·완료 이력을 연쇄 로드하지 않는다. 근거가 불명확할 때만 해당 페이지/항목을 추가 확인한다.
2. Current 하나를 구현·검증한다. 페이지 개선은 공식 참조/before/after, 세 폭 전체 구성과 실제 키보드로 판정한다. 기능 pass만으로 사용성 완료 처리하지 않는다.
3. 제품 변경 시 관련 회귀, `npm.cmd run lint -w apps/web`, `npm.cmd run build -w apps/web`를 수행한다. 증거는 `docs/ux-evidence/<ID>.md`에 남긴다. 미완료면 재개 조건을 남기고 Current 유지, 완료면 다음 미완료로 이동한다.
4. 완료 작업의 긴 기록은 증거/이력에만 보관한다. 큐에는 누적하지 않는다. UI-021에서 새 결함 발견 시 중복 확인 후 범위·완료 기준을 가진 단위로 편성하고 그 실행에서는 구현하지 않는다.
5. 외부 준비 대기는 해당 경로만 막는다. 사용자 DB 초기화·새 인프라/권한·예약 변경 금지. 실제 quota/rate limit 감지 시 즉시 저장소 변경 중단, 자동 재설정/구매 금지.

필요할 때만: [기존 실행 규칙·외부 준비·편성 이력](./NEXT_ACTIONS_HISTORY_2026-09-23.md). 이력의 과거 큐/Current는 실행 지시가 아니다. UI-021 진입 시에만 이력의 UI-021 및 E01–E04 기준을 읽는다.

# UI-021 — 차단 범위 수정 후 실행 재개

2026-09-20. **부분 검증 완료, 통합 gate 미완료, 새 결함 UI-021-F01 → UI-064 편성.**

## 이번에 해결한 진행 차단

- E01/E03을 전체 실행 선행 조건으로 둔 NEXT_ACTIONS와 상위 문서의 모순을 수정했다. 첨부 성공/사람 수용 판정만 각각 대기하며 가능한 화면·실키·재조회는 진행한다.
- 새 명세 위반은 중복 확인 후 수정 단위를 만들고 Current로 전환한다. 매번 편성 승인을 다시 요구하지 않는다. 새 기능/외부 권한/비용은 여전히 별도 확인한다.
- 최초 브라우저 자동 검증 시 `localhost:5173` 연결 거부. API health는 정상이고 별도 HTTP 확인에서도 웹 포트만 연결 거부였다. 서버/DB를 재시작하지 않고 `npm.cmd run preview -w apps/web -- --host 127.0.0.1 --port 5173 --strictPort`로 방금 빌드한 웹을 복구했다. **빌드가 DB를 지운다는 근거는 없으며 fixture는 복구 후 그대로 있었다.**
- 최초 실패 진단 [JSON](./ui-021-resume-live.json)은 연결 거부 기록일 뿐 UI 검증 통과 자료가 아니다. 아래 성공 조작은 이후 Codex in-app browser에서 수행했다. 미완성 진단 스크립트는 제거했다.

## 빌드·fixture·경로

- HEAD `0bf43ff` + 기존 사용자 미커밋 변경. 제품 소스 수정 없음. 웹 산출물 `index-CTg_lz79.js` / `index-DgysQuew.css`를 preview로 검증.
- Web `http://localhost:5173`, API `http://localhost:4000`, 로그인 로컬 Admin.
- 전용 Project **UI-021 Resume 1789875892**, `projectId=1`, Master Suite, Authentication > Login, Run **Resume verification**, `runId=1`.
- API로 C1 Valid tester can sign in / C2 Invalid password remains on login / C3 Sign out removes session 생성. 사전조건 2문장, Action–Expected 3쌍, 공통 Expected, refs `UI021-RESUME`. **API seed이므로 J02 UI 작성 증거가 아니다.** 지침 내용이 같은 fixture여서 다른 테스트의 서로 다른 지침 검증도 아직 아니다.
- 최초 3건 모두 Untested. 실조작 후 C1 Passed(결과2), C2 Failed(결과1), C3 Untested. 사용자 기존 프로젝트를 삭제·초기화하지 않았다.
- 실행 서버는 실제 결과1의 presign 응답과 `results.routes.ts`의 `!deps.prisma` 분기로 in-memory 제한을 확인했다. shell 환경변수 없음으로 서버 모드를 단정하지 않았다. 이번 재조회는 서버 프로세스 내 보존만 증명하며 DB 재시작 영속성 증거가 아니다.

## 실제 화면·키보드·API 재조회

| 부분 경로 | 이번 관찰 | 판정 범위 |
| --- | --- | --- |
| J04 지침 읽기 | C1 상세에서 Preconditions, 3 Action/Expected, Expected result 확인. 결과 입력 폼은 상시 노출되지 않고 Add result로 연다 | 이 fixture 읽기 pass; Text/BDD/10-step/서로 다른 지침 통합 미실행 |
| J05 패널 진입 | Add result → 공통 2열 dialog, Passed 기본값. Status에서 실제 Tab → Comment, Shift+Tab → Status, Escape → 닫힘 및 원래 Add result 초점 복귀 | 해당 키 경로 pass; 전체 키 순환·오류 상태 미검증 |
| J05 취소 | 위 취소 후 C1 `GET /api/tests/1/results` total=0 | 결과 무생성 pass; 이번에는 네트워크 POST 0회 추적까지 수행하지 않음 |
| J05 목록 진입/저장 | C2 Status → Failed → 같은 dialog, 대상 C2/Failed 표시. Comment·Version·Defects `AUTH-21` 입력, Defects에 Enter 없이 Add Result | 정확한 대상·일반 저장 유지·결함 draft 보존 pass |
| J05 재조회 | C2 결과1: failed, comment `Resume check: invalid password kept the login page.`, version `resume-20260920`, defects `[AUTH-21]`, total=1 | 실제 API 재조회 pass; 첨부/담당자 성공은 포함하지 않음 |
| J04 명시적 다음 | 390px C1 Status → Passed → Comment `Mobile explicit next verification.` → More save actions → Save & Next. URL testId=1→2, C2 Failed 상세로 이동. 통계 Passed1/Failed1/Untested1 | 모바일 명시적 다음 pass; 일반 저장 자동 이동으로 잘못 해석하지 않음 |
| J08 모바일 복귀 | 390×844 C2 Back to tests → testId 없음, 3개 행 높이 37/37/36.5px, 목록 유지 | UI-052 원 관찰의 목록 복귀 경로 pass |
| J08 모바일 dialog | bbox x12/y20/width366/height812, viewport390×844, 문서 scrollWidth390. Add Result/Cancel 고정 하단 노출, 입력은 내부 스크롤 | 가로 넘침 없음·저장 진입 가능. 전 기기/스크린리더 합격 아님 |
| J08 모바일 통계 | 3열 범례에 상태명 생략. Passed16/37px, Untested16/49px (client/scroll) | **fail UI-021-F01, UI-064 수정 Current** |
| E01 첨부 성공 | 실제 결과1에 `{fileName: ui021-check.txt, contentType: text/plain}` presign → HTTP501 `STORAGE_UNAVAILABLE` | blocked, 정상 업로드·원본 bytes 다운로드 주장 금지 |
| E03 사람 수용 | 독립 테스터의 목적만 제시한 관찰 없음 | pending. 다른 실행 경로 중단 사유가 아님 |

### 화면

- [1280×720 지침/Run](./ui-021-resume-run-1280x720.png)
- [1280×720 공통 결과창](./ui-021-resume-dialog-1280x720.png)
- [390×844 목록 복귀](./ui-021-resume-list-390x844.png)
- [390×844 공통 결과창](./ui-021-resume-dialog-390x844.png)
- **UI-021-F01 수정 전:** [390×844](./ui-021-f01-before-390x844.png), [1280×720](./ui-021-f01-before-1280x720.png). 같은 1 Passed/1 Failed/1 Untested 데이터, 모바일 상세 닫힘·scrollY0. 제품 수정이 없으므로 after를 만들지 않는다.

## 자동 검증

- `npm.cmd run lint -w apps/web` — exit 0.
- `npm.cmd run build -w apps/web` — 첫 실행은 sandbox 상위 경로 접근 거부. 승인된 동일 명령 재실행 exit 0, Vite 481 modules. 500kB 초과 chunk 경고는 남음. DB/fixture는 남아 있음.
- `node node_modules/vitest/vitest.mjs run apps/web/src/features/runs/utils/runStatusOverview.test.ts apps/web/src/features/runs/utils/resultSaveAdvanceIntent.test.ts apps/web/src/features/runs/utils/resultPartialRecoveryModel.test.ts apps/web/src/features/runs/utils/resultSaveLifecycle.test.ts` — **34 passed**. Helper 성공이 범례 시각적 가독성을 검증하지 못함을 이번 fail로 확인.
- 프로세스 한정 `USE_IN_MEMORY_REPOSITORY=true`; `npm.cmd run test -w apps/server -- src/__tests__/result-attachment-storage.test.ts src/__tests__/phase3-runs-results.test.ts src/__tests__/result-correction-policy.test.ts` — **8 passed, 1 skipped**. 기존 수정 권한 테스트의 skip은 pass로 계산하지 않음. 개발 DB 미사용.

## UI-021-F01 → UI-064 인계

- 기존 UI-040 계약: 좁은 화면 상태명·건수·비율을 텍스트로 읽고 조작, 첫 행 접근 유지. [설계](../RUN_STATUS_OVERVIEW_DESIGN_2026-09-19.md) §5는 두 열 등을 명시한다.
- 원인: `RunStatusOverview.tsx`에서 고정 80px 차트 옆 3열, 상태명 `truncate`. 버튼73px, 텍스트가 16~32px만 차지. aria-label 전체 제공만으로 시각 사용자 문제가 해결되지 않는다.
- 현재 미완료 수정 단위 중 중복 없음. 다음 미사용 ID UI-064를 상위 문서에 추가하고 Current로 이동. UI-021은 unchecked로 다음 후보 유지. 이번 검증 실행에서 UI-064 구현까지 확장하지 않는다.

## 이후 남은 실행

1. **UI-064 구현·실화면 검증** (E01/E03 대기 불필요).
2. UI-021 복귀 시 F01 재검증 후 20+ 케이스·3단계/동명 섹션·서로 다른 지침/템플릿·활성 A/B·완료 Run·Plan fixture의 J01/J02/J03/J06/J07 및 남은 J04/J05/J08을 수행한다. 이번 세 건 fixture로 전체 19개 UX 체크를 완료하지 않는다.
3. E01: 운영자가 사용할 기존 **분리된 테스트용 Prisma DB + 실제 upload/download endpoint**를 확인해야 실제 bytes 성공을 검증할 수 있다. 현재 in-memory에 URL만 넣어도 presign의 Prisma 조건을 통과하지 않는다. DB 초기화·임의 외부 인프라 생성·구매를 하지 않았다.
4. E03: 사람에게 J01–J08의 목적만 전달한 관찰·수용 기록이 마지막에 필요하다. 실행자가 대신 승인하지 않는다.

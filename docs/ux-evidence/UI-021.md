# UI-021 — Cross-route visual and accessibility regression gate

Date: 2026-09-20 (continued) · **2026-09-23 authoring + exec reverify**  
Result: **not complete**. Checkbox stays `[ ]`. No product code changed in the 2026-09-23 verification runs.

## 최신 실행 — 2026-09-23 (2) 실행·재개 경로 (UX-002/030/031/040, UI-052/055/056)

Verification-only. Script: [`_ui021-exec-reverify.py`](./_ui021-exec-reverify.py) → [`ui-021-exec-reverify.json`](./ui-021-exec-reverify.json) **allOkForThisPass true**, **newContractDefects []**.

| Path | Verdict | Notes |
| --- | --- | --- |
| No Jump checkbox | **pass** | UI-056 |
| Add Result stays on testId | **pass** | UX-002 / UI-051 |
| Pass & Next advances + next instructions | **pass** | B→C; last test no wrap |
| Bulk Apply list STATUS sync | **pass** | D/E → Blocked without reload (UI-055) |
| Hub / Overview / My Tests | **pass** | Run visible; overview counts; my-tests reachable |
| 390 Back to tests sticks | **pass** | URL without `testId`, no bounce (UI-052) |
| 390 no auto-seed | **pass** | `/runs/:id` stays list |
| Loop 8 new defect | **none** | — |

Remaining runnable non-E paths from the prior authoring pass table are now covered. **Only E01 + E03 block UI-021 closure.**

### Readiness reconfirm (this turn — no repeat of authoring/exec captures)

| Check | Status |
| --- | --- |
| API `4000` / Vite `5173` | ok |
| `STORAGE_PUBLIC_BASE_URL` | **unset** → E01 still blocked |
| Independent tester observation | **absent** → E03 still blocked |

**2026-09-23 readiness-only recheck:** same — `STORAGE_PUBLIC_BASE_URL` unset, API/Vite ok, no tester observation file. Authoring/exec scripts not re-run (Loop 6).

**2026-09-24 readiness-only recheck:** same — process `STORAGE_PUBLIC_BASE_URL` unset, no `.env` with storage config, no tester/J01–J08 observation file under `docs/`. Authoring/exec scripts not re-run (Loop 6).

**User actions required (no auto-purchase / no secret print):**

1. **E01:** Provide a test attachment store + account that can presign/upload/read/download real bytes for case (UI-071) and result (UI-031/057) paths.
2. **E03:** Have a designated tester record unaided J01–J08 observation (§8.3 / UX-041).

## 이전 실행 — 2026-09-23 (1) UI-064/F01 + UI-065–072 작성→읽기→Run

Verification-only. Script: [`_ui021-authoring-reverify.py`](./_ui021-authoring-reverify.py) → [`ui-021-authoring-reverify.json`](./ui-021-authoring-reverify.json) **allOkForThisPass true**, **newContractDefects []**, **canCompleteUi021 false**.

### Fixture

- In-memory API `localhost:4000`, Vite `5173`, `admin@example.com`.
- Project **UI-021 Reverify …**, section Login + Checkout, run F01 legend (Passed/Failed/Untested) + authoring run with Text+Steps cases.

### Checks this pass

| Path | Verdict | Notes |
| --- | --- | --- |
| UI-064 / F01 legend @390 | **pass** | Passed/Failed/Blocked/Retest/Untested/All statuses full text, no ellipsis, no overflowX. [capture](./ui-021-reverify-f01-legend-390x844.png) |
| UI-066 dirty leave | **pass** | Cancel after title edit opens discard dialog |
| UI-071 staging on Add | **pass (delivery)** | Staging panel present; **real bytes still E01** |
| UI-072 list return | **pass** | `return` on Add; after save restores q/priority/scope/groupBy/display |
| UI-067 Text save/reopen | **pass** | Preconditions/Steps/Expected + refs `JIRA-UI021` on API + case panel |
| UI-067 Steps Add & Next | **pass** | Second case created; form stays on Add with success notice |
| UI-070 read hierarchy | **pass** | Panel shows PRECONDITIONS → STEPS → EXPECTED (refs visible) |
| Run panel Text instructions | **pass** | Selected test shows authored preconditions/steps/expected. [capture](./ui-021-reverify-run-instructions-1280x720.png) |
| Run panel Steps instructions | **pass** | Guest-email steps readable on second test. [capture](./ui-021-reverify-run-steps-1280x720.png) |
| New contract defect (Loop 8) | **none** | No new UI-073+ scheduled |

### UX / J separation (authoring pass; exec pass updated above)

| Gate / journey | Verdict | Remaining |
| --- | --- | --- |
| UX-001 / UX-021 / J02 author | **pass** | — |
| UX-020 / J04 read→perform | **pass** | — |
| UX-012 leave | **pass** | Attach partial = E01 |
| UX-022 / UI-072 context | **pass** | — |
| UX-034 / UI-064 legend | **pass** | — |
| UX-002 / 031 save vs next | **pass** (exec pass) | — |
| UX-030 bulk sync | **pass** (exec pass) | — |
| UX-040 hub/overview/my-tests | **pass** (exec pass) | — |
| UX-033 / J06 attach bytes | **blocked E01** | storage unset |
| UX-041 / J01–J08 human | **blocked E03** | tester observation |

### Closure blockers (unchanged owners)

- **E01:** configured attachment storage for original-byte upload→reopen→download (case attach UI-071 path + result attach UI-031/057 debt).
- **E03:** unaided tester/design observation (section 8.3 / UX-041).

2026-09-24 통합: 위 기록은 보존한다. 실행 순서는 NEXT_ACTIONS로 이동했으며 UI-021은 PX/UG 후 최종 수용으로 남는다. E01/E03은 미해소이고 작성·실행 성공만으로 최종 완료하지 않는다. 이전 전체 기록은 Git 이력에 보존한다.


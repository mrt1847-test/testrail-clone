# UI-021 — Cross-route visual and accessibility regression gate

Date: 2026-09-20 (continued same day)  
Result: **not complete**. Checkbox stays `[ ]`. Current batch stays UI-021. No product code changed.

This gate is verification-only. Component delivery and prior UI-037–051 checks are not integrated acceptance.

## Fixture (in-memory)

- API `localhost:4000` (`USE_IN_MEMORY_REPOSITORY` not `"false"`), Vite `http://localhost:5173`, role `admin@example.com` / `password`.
- Project 1 **UI-021 Checkout Extremely Long Release Name That Should Truncate**, suite Master `id=1`.
- Sections: General(1), Checkout(6), Payment(7), Checkout/Login(8), Authentication(9), Authentication/Login(10). Milestone **Sprint 9**.
- Newly authored (UI, not seed titles):
  - **C1** Text in Payment — “Receipt PDF prints the billed total”. Preconditions/Steps/Expected persist on reopen (`GET /api/cases/1`). `refs` stayed `null` after JIRA-UI021.
  - **C2** Steps in Authentication/Login — “Guest checkout email is unique”, 3 Action/Expected pairs + common expected. Persist in case read and Run panel.
- Supporting instruction cases C3–C5 via API for volume.
- Run **R1 Alpha regression** (`/projects/1/runs/1`): Selected cases, 5 tests, static membership. After J07 bulk Blocked on C3/C4/C5: C1 Passed, C2 Failed, C3/C4/C5 Blocked (`GET /api/runs/1`). C3 has two results (`id=3` failed + `id=6` blocked). After moving C5/C6/C7, Alpha stayed **5** (C1–C5).
- Plan **UI-021 Checkout plan** `id=1789831508338`, entry Checkout browsers (C1–C3), generated run `id=2`.
- Nightly **R3** closed via UI after **Sync now** (`+2 / -0`, instances 1–7). Record buttons (Add result / Pass & Next) gone; badge **CLOSED**.
- Storage: `STORAGE_PUBLIC_BASE_URL` defaults to `https://storage.local`. In-memory presign remains 501 `STORAGE_UNAVAILABLE`. Production `npm.cmd run build -w apps/web` was not run (wipes this fixture).

## Whole-flow (live)

1. Authored C1 Text and C2 Steps in the repository, then selected those cases (plus C3–C5) into Alpha.
2. Read C1 instructions in the Run panel. Cancel Add Result: stayed 5/5 untested.
3. Saved C1 **Passed** from panel Add Result (comment “Receipt total matched 12.00 USD”). **Jump to next after save was checked but URL stayed `testId=1`.** Manual Next opened C3 (SKU-41 instructions).
4. Saved C2 **Failed** from list Status → same UI-051 dialog (Assign To Admin, comment “Confirm email still showed a mismatch”). Latest: failed. Stats **20% passed · 3/5 untested**.
5. Reopened C1 on `/projects/1/cases/1`: same preconditions/steps/expected. UI-051 “Could not load case details” did **not** reproduce on C1/C2.
6. Resume: hub shows Alpha 20%; My Tests shows C2 Failed in R1; Plan has **Open run** for the generated 3-test run; Overview lists both runs + plan.

## Viewport / a11y (this build)

| Surface | 1440×1000 | 1280×720 | 390×844 | `overflowX` |
| --- | --- | --- | --- | --- |
| Test Cases | captured | captured | captured | 1280/390 previously false |
| Run Execution (populated) | captured | captured | stacked + Back to tests | false at 1440 (`scrollWidth` 1425), 1280 (1265), 390 (390) |
| Add Result dialog | — | captured | — | false at 1280 (UI-051) |
| Runs hub | — | captured | captured | 390 false |
| My Tests | — | captured | captured | 390 false |
| Plan | — | captured | captured | 390 false |
| Overview | — | captured | captured | 390 false |

- Header long name is clipped: visible “tremely Long Release Name That” (leading **Ex** missing). Gate **clipped controls = fail**.
- Selected-row bar: **1 selected** / Apply result / Assign / Clear selection visible (not hidden).
- Run select-all checkbox accessible name **`on`** at 1280 snapshot. Row checkboxes `Select C1`…`Select C5`. Discussion textareas unlabeled. Native Tab/Shift+Tab not used (Electron routes to Cursor UI); scripted `.focus()` only.
- 390 Run screenshot tool paints a wide canvas; CSS `innerWidth` was 390. Opening `/runs/1` with no `testId` **auto-appends `?testId=4`**. Back to tests briefly drops `testId`, then bounces back to the C4 card. List checkboxes exist in the a11y tree with **height 0**.
- Duplicate CTAs: repository header Add Case + quiet per-block Add Case (supersession: intentional). Hub Add Plan + Add Run (UI-050: intentional). Not scored as duplicate-dominant failure.

## J01–J08

| Journey | Verdict | Proof / gap |
| --- | --- | --- |
| J01 find/organize | **pass with notes** | Authentication `sectionId=9`: **direct 0 / subtree 1 (C2) / all 5**. Duplicate Login distinguished as Checkout / Login (C3) vs Authentication / Login (C2). Tree **6 sections · 5 cases**. Earlier stale “2 cases” live region still a defect. |
| J02 author | **pass with defect** | Text/Steps authored, saved, reopened. References field dropped (`refs: null`). |
| J03 membership | **fail** | Selected Alpha stayed **5** after C6/C7 and after moving C5 (correct). Reopen of Nightly still 5 until **Sync now** (`Composition synced: + 2 / - 0`, then C6+C7, 7 tests). Sync now sits under Composition and was covered by a sticky Run discussion (click intercepted). Dynamic still not re-synced this turn. Labels still imply automatic live membership. |
| J04 read/perform | **pass** | C1 prose + C2 numbered Action/Expected readable beside the list. Instruction-load exception not seen on this fixture. |
| J05 record | **fail** | Panel C3 Failed with comment, Assign Admin, Version `build-21`, Defects `CART-21`, two staged files. `GET /api/tests/3/results`: comment+version persist, **`defects: []`**, one result `id=3`. Jump to next still did not leave `testId=3`. |
| J06 recover | **blocked / fail** | Live: **Result saved. Couldn't attach 4 files. Retry** and **Attachment storage isn't available** on `cart-empty.txt`. `GET /api/results/3/attachments` `[]`. Duplicate queued rows (4 files) from double-staging. After leave/reopen C3, **Retry was gone**; Latest failed comment remained. Configured-storage byte roundtrip still impossible. |
| J07 bulk/scope | **fail** | Bulk Apply still lies until reload (prior). Live **Copy or move**: C7+C5+C6 from General/Checkout/Payment → Authentication. Toast **Moved 3 selected cases.** API: C5/C6/C7 `sectionId=9`; C1/C2/C3/C4 unchanged. Chooser Target section lists two **— Login** without parent path and indents Payment under General. Bulk failure/retry still not injected. |
| J08 resume | **fail** | 390 hub/My Tests/Plan/Overview `overflowX` false. **Back to tests does not stick:** `/runs/1` auto-opens `testId=4`; after Back, URL returns to C4 card; list row height 0. Empty: project 3 Cases “No test cases…”, hub “No test runs yet”. Failure: `/runs/99999` “Run not found / Please try again / Retry” at 390 and 1280; Retry stays on the same error with focus on Retry. Native Tab still not used (DOM Tab only). |

## UX ownership table (integrated 2026-09-20)

| Gate | Verdict | Evidence |
| --- | --- | --- |
| UX-001 | **fail** | Instructions roundtrip, but References do not persist; not a complete authored case. |
| UX-002 | **fail** | Selected test/URL match while idle; Jump to next does not move after save. |
| UX-003 | **partial** | Filters/status/selection explainable; stale tree counts and false scope live region. |
| UX-010 | **pass** | Hub, Overview, My Tests, two result entries. |
| UX-011 | **pass** | Case read vs Edit vs Run record stay distinct on C1/C2. |
| UX-012 | **partial** | Cancel/discard and dialog fields named; native Tab unverified; unlabeled select-all. |
| UX-013 | **pass** | Quiet tree, section blocks, instruction panel, compact dialog. |
| UX-020 | **pass** | C1 found by title; case page returns via Back to cases. |
| UX-021 | **fail** | Text/Steps persist; refs dropped. |
| UX-022 | **pass with fixture limit** | Live 0/1/5 on Authentication (not the 2/6/10 review counts; this project has 5 then 7 cases). Compact/density not confused with scope. |
| UX-023 | **pass** | Parent/child Login under Checkout vs Authentication distinguishable. |
| UX-030 | **fail** | 3-selected bar and Apply result are visible. After save, list STATUS lied until reload; bulk failure/retry not run. |
| UX-031 | **fail** | Same small form from both entries; jump-next contract broken. |
| UX-032 | **blocked** | Attachment/assignment partial retry not re-run; storage unavailable. |
| UX-033 | **blocked** | No in-repo byte store. Safe 501 is not the success path. |
| UX-034 | **partial** | Top stats + readable instructions; next-instruction automation failed. |
| UX-040 | **partial** | Hub/My Tests/Plan/Overview populated at 1280 and 390; Sprint 9 still 0 active runs; Overview “16 remaining” does not match 7 cases. |
| UX-041 | **blocked** | No independent tester observation (section 8.3). |
| UX-042 | **fail** | Clipped header; unlabeled select-all; native keyboard missing; 390 list never stays open after Back to tests. Empty/failure copy is readable. |

Section 8.3 human 5-second orientation: **pending** (cannot be automated).

## Automated checks

- `npx vitest run src/features/runs/utils/resultEntryDialogModel.test.ts src/features/runs/utils/runListStatusEntry.test.ts src/features/runs/utils/resultSaveLifecycle.test.ts` — 28 passed.
- `npx tsc -p tsconfig.app.json --noEmit` in `apps/web` — passed.
- `npm.cmd run build -w apps/web` **not run** (would rebuild shared and wipe in-memory fixture).

## Proposed repair units (not queued, not implemented)

1. **Header long-name clip** — global project switcher truncates the first letters of a long name at 1280/1440. Repro: this fixture name. Accept: full name or an accessible truncated form with tooltip, no missing leading characters.
2. **Jump to next after save** — checked Jump to next, save C1 Passed, URL stayed `testId=1`. Accept: ordinary save stays; explicit next / checked jump advances to a different test with different instructions.
3. **Case References persist** — UI value JIRA-UI021; `GET /api/cases/1` `refs: null`. Accept: save/reopen/API match.
4. **Run select-all accessible name** — checkbox name `on`. Accept: “Select all on this page” (already present at 390 in one scan).
5. **Configured-storage byte roundtrip** — environment blocker for UX-033. Not an API invention inside UI-021.
6. **Milestone linked work** — Sprint 9 shows 0 active runs while Alpha exists. Fixture or linking defect; needs an explicit unit if product-side.
7. **Live All/Dynamic membership** — Nightly `include_all_live` and Dynamic high did not add C6/C7 after repository create + UI reopen. Accept: All includes new suite cases; Dynamic high includes new high C6 and excludes low C7; Selected stays fixed.
8. **Result Defects persist** — UI `CART-21`; `GET /api/tests/3/results` `defects: []`. Accept: save/reopen/API match like Version/comment.
9. **390 Run list after Back to tests** — `/runs/1` auto-selects `testId=4`; Back to tests bounces back to the C4 card; list row height 0. Accept: Back to tests (or opening with no testId) shows a usable first list row without horizontal overflow, and does not re-open the previous test by itself.
10. **Run list status after bulk apply** — after Apply Blocked on 3 tests, toast and run-wide stats updated, but list STATUS and selected-test header stayed prior values until reload. Accept: selected rows, header chip, and Latest all match the new status without a full page reload.
11. **Move/Copy target section labels** — dialog options `— Login` twice and `— Payment` under General. Accept: each option shows parent path (Checkout / Login vs Authentication / Login).
12. **Live All membership vs Sync now** — creating/reopening Nightly did not pick C6/C7; Composition **Sync now** did (`+2 / -0`) but is below the fold and click-intercepted by Run discussion. Accept: All/live either updates on repository change or exposes a named, unobstructed Sync in the run header.

## Screenshot review

![Test Cases 1440×1000](./ui-021-cases-1440x1000.png)

![Test Cases 1280×720](./ui-021-cases-1280x720.png)

![Test Cases 390×844](./ui-021-cases-390x844.png)

![Run 1440×1000 populated](./ui-021-run-1440x1000.png)

![Run 1280×720 populated](./ui-021-run-1280x720.png)

![Run 390×844 stacked](./ui-021-run-390x844.png)

![Add Result dialog 1280](./ui-021-result-dialog-1280x720.png)

![Bulk 1 selected 1280](./ui-021-run-bulk-1280x720.png)

![Runs hub 1280](./ui-021-hub-1280x720.png)

![Overview 1280](./ui-021-overview-1280x720.png)

![My Tests 1280](./ui-021-my-tests-1280x720.png)

![Milestones 1280](./ui-021-milestones-1280x720.png)

![Plan 1280](./ui-021-plan-1280x720.png)

![C1 case read 1280](./ui-021-case-c1-1280x720.png)

![Reports 1280](./ui-021-reports-1280x720.png)

![Settings 1280](./ui-021-settings-1280x720.png)

![J01 Authentication subtree](./ui-021-j01-auth-subtree-1280x720.png)

![J01 All sections](./ui-021-j01-auth-all-1280x720.png)

![J03 Nightly all-cases 5 tests](./ui-021-j03-nightly-all-1280x720.png)

![J03 Dynamic High form](./ui-021-j03-dynamic-form-1280x720.png)

![J03 Dynamic after new cases still 3](./ui-021-j03-nightly-after-new-cases-1280x720.png)

![J06 C3 attachment 501 retry](./ui-021-j06-c3-attachment-retry-1280x720.png)

![J06 C3 reopen no Retry](./ui-021-j06-c3-reopen-no-retry-1280x720.png)

![J07 bulk before Apply (screenshot tool lagged; CDP had 3 selected)](./ui-021-j07-bulk-before-1280x720.png)

![J07 immediately after Apply — toast vs stale list](./ui-021-j07-bulk-after-1280x720.png)

![J07 after reload — C4/C5/C3 Blocked, C1 Passed](./ui-021-j07-bulk-after-reload-1280x720.png)

![J07 after moving C5 C6 C7 into Authentication](./ui-021-j07-move-after-1280x720.png)

![J08 Nightly CLOSED](./ui-021-j08-nightly-closed-1280x720.png)

![J08 390 Runs hub](./ui-021-hub-390x844.png)

![J08 390 My Tests](./ui-021-my-tests-390x844.png)

![J08 390 Plan](./ui-021-plan-390x844.png)

![J08 390 Overview](./ui-021-overview-390x844.png)

![J08 390 after Back to tests — still C4 card](./ui-021-run-390-list-first-row.png)

![Empty Cases 390](./ui-021-empty-cases-390x844.png)

![Empty Runs hub 390](./ui-021-empty-runs-390x844.png)

![Run not found 390](./ui-021-run-not-found-390x844.png)

![Run not found 1280](./ui-021-run-not-found-1280x720.png)

## Not live-verified / blocked

- Tester/design review and section 8.3 unaided orientation.
- Configured object-storage upload → refresh → original-byte download (501 retry was live; bytes were not).
- Native Tab / Shift+Tab cycle.
- Bulk failure/retry without duplication.
- Dedicated still of the brief “Loading run…” / “Loading the test case workspace…” frames (text seen; no saved still).
- Browser Back/refresh timing after C3 save (Escape closed dialog; dedicated refresh not recaptured).
- `ui-021-j05-c3-dialog-1280x720.png` is a stale session spinner; use the J06 retry capture for the filled form.
- `npm run build -w apps/web` skipped to keep the in-memory fixture.

UI-021 remains unchecked until every applicable UX gate and section 8 requirement passes.

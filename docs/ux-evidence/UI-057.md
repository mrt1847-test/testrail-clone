# UI-057 — 부분 실패 초안/재시도 유지

Completed: 2026-09-20

## Result

- 결과 저장 후 첨부만 실패해도 **Leave**로 창을 닫으면 Retry·실패 파일이 같은 세션에 남는다.
- **Discard unfinished**만 미완료 첨부를 폐기하며, 저장된 결과는 유지한다.
- A 닫기 → B 열기 → A/B 재열기에서 결과 수가 늘지 않고 같은 `createdResultId`로 복구한다.
- 새로고침 등으로 File bytes를 잃으면 sessionStorage 메타로 재선택 안내를 유지한다 (실제 저장소 bytes 왕복은 E01).

## Reproduction

- Fixture: project **UI-057 Partial Recovery**, run R1, C1/C2.
- Before (historical J06): leave/reopen dropped Retry — [ui-057-before-1280x720-j06-retry-lost.png](./ui-057-before-1280x720-j06-retry-lost.png).
- Live before fix path: Cancel always called `closeResultDialog(true)` → `lifecycle.cancel()` cleared recovery.
- Live after: C2 Failed + 2 files → `Result saved. Couldn't attach 2 files. Retry` (`createdResultId=3`). Cancel → Leave → Prev(C1) → Next(C2) → Add result: recovery `3`, 2 failed files, comment kept, Retry present. `GET /api/tests/2/results` total **2** (no duplicate on reopen).

## Change

- `ResultEntryDialog`: partial-success close prompt with Keep editing / Leave / Discard unfinished.
- `RunDetailPage`: `onClose({ discard })` only cancels on explicit discard; hydrates `recoveryDraft` + recovery status; parks/restores via sessionStorage.
- `resultPartialRecoveryModel` + lifecycle `restoreParked` / awaiting file reselect without new result.

## Verification

- Unit: `resultPartialRecoveryModel` + `resultSaveLifecycle` — 27 passed.
- Live 1280×720: partial Retry → Leave → A↔B → reopen Retry+files ([after](./ui-057-after-1280x720-reopen-retry.png)).
- Live 390×844: `overflowX=false`, Retry named, reselect/recovery message readable ([after](./ui-057-after-390x844-reopen-retry.png)).
- lint/build apps/web — see command log.

## Exception

- Native Tab cycle not used (E02/UI-062).
- Assignment-only live injection not repeated (covered by lifecycle unit test).
- Configured storage byte roundtrip still E01/UI-021; mock 501 is not attachment success evidence.

# UI-058 — All/Dynamic 포함 규칙 정합성

Completed: 2026-09-20

## Result

- All (`include_all_live`): 새 케이스 생성 직후 수동 Sync 없이 런에 포함.
- Dynamic high: 새 high 포함, low 제외.
- Selected (`static`): 새 케이스 미포함, Sync는 skipped/static, 기존 결과 유지.
- Header에 **Sync now** (`aria-label="Sync composition"`) + Membership 라벨. Discussion에 가리지 않음. Selected에는 Sync 없음.

## Reproduction

- Before (UI-021 J03): Nightly All 재열기만으로 C6/C7 미포함, Sync는 Composition 접힘 아래·Discussion에 가림. Dynamic도 갱신 미검증. [before All header](./ui-058-before-1280x720-all-header-no-sync.png), [before Dynamic stale](./ui-058-before-1280x720-dynamic-stale.png).
- Cause: in-memory에서 case create 후 `compositionSync`가 없어 live suite sync가 no-op. Sync UI는 접힌 Composition에만 있음.

## Change

- `RunsService.syncLiveRunsForSuite` + in-memory `syncRunComposition` 경로로 suite live sync.
- `cases.routes`: create/duplicate/update/delete 후 `runsService` fallback sync.
- `RunExecutionHeader`: live 모드에 Membership + Sync now; 상단 sync feedback.

## Verification

- Server: `npm.cmd run test -w apps/server -- run-composition.test.ts` — 6 passed / 2 skipped (in-memory membership 포함).
- `npm.cmd run lint -w apps/web`, `npm.cmd run build -w apps/web`, `npm.cmd run lint -w apps/server`, `npm.cmd run build -w apps/server` — pass.
- Live API (`ui-058-api-verify.json`): allOk/dynOk/selOk, histCount=1.
- Live UI 1280×720 All: Sync visible, not covered, 6 tests incl. C6–C8 — [after](./ui-058-after-1280x720-all-sync-header.png).
- Live UI 390×844: Sync visible, `overflowX=false` — [after](./ui-058-after-390x844-all-sync-header.png).
- Dynamic 3 high only — [after](./ui-058-after-1280x720-dynamic.png). Selected 2 fixed, no Sync, result kept — [after](./ui-058-after-1280x720-selected.png).
- Sync feedback — [after](./ui-058-after-1280x720-sync-feedback.png). Details: `ui-058-live-verify.json`.

## Exception

- Native Tab cycle not used (E02/UI-062). Focus/a11y name checked via roles/`aria-label`.
- Prisma live path covered by existing skipped integration tests when DB available; this run used in-memory.

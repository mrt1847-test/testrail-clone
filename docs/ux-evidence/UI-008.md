# UI-008 — Run Execution workbench header

Completed: 2026-09-18

## Result

- Replaced the stacked run title, assignment, state, and secondary-action rows with one compact workbench header.
- Kept the execution table and result composer as the work surface immediately below the header; result recording is not included in the utility menu.
- Consolidated run management, reports, print, and CSV exports under the keyboard-accessible `Run utilities` overflow. Existing defect actions remain grouped there, and report coverage retains Traceability.

## Verification

- Desktop review at 1280×720: page `scrollWidth` was 1265px within a 1280px viewport; the test table began at approximately 323px from the viewport top (baseline was about 545px).
- Mobile review at 390×844: page `scrollWidth` was 375px within a 390px viewport; the title/status, assignee, copy control, and utility trigger wrapped into distinct, usable rows without clipping.
- Utility-menu inspection confirmed grouped Run management, Reports, Output, and Defects actions. `Escape` closed the menu and restored focus to `Run utilities`.
- `npx.cmd vitest run apps/web/src/features/runs/utils/runExecutionHeaderMenu.test.ts`
- `npm.cmd run lint -w apps/web`
- `npm.cmd run build -w apps/web` (passed; existing Vite large-chunk warning remains).

## Screenshot review

Desktop and mobile screenshots were captured during the local run-workbench review. The temporary local fixture was reset by the development server before persistent image artifacts could be retained; the measurements and accessibility checks above record the verified visual evidence.

UI-033 remeasured the missing header baseline on 2026-09-19 with default section tree and result pane open at 1280×720: `#run-tests-section` started at y=239, the table at y=306, and the first test row at y=361. Captures: [UI-033 evidence](./UI-033.md).

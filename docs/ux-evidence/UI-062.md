# UI-062 — Run 제어 이름과 실제 키보드 조작

Completed: 2026-09-20

## Result

- 전체 선택 체크박스 접근 이름: **Select all tests on this page** (`aria-label` + `title`). UI-021의 `on` / title-only 경로 해소.
- Discussion: **Test discussion comment** / **Run discussion comment**가 `label htmlFor`로 textarea에 연결 (`CommentComposer`).
- E02 Playwright Chromium 실키: Status 메뉴 → Failed → 결과 dialog → Tab/Shift+Tab → Escape → Discard draft → **원래 Status 트리거로 포커스 복귀**.
- 닫힌 Run: Status 메뉴 0. Passed 칩 필터 시 행 0. 기본 fixture에 필수 커스텀 필드 없음(저장 N/A). 390×844 가로 넘침 없음.

## Reproduction

- Before (`ui-062-repro.json`): select-all `aria` null / title만, Discussion textarea `aria`·`labelFor` null.
- Focus restore 실패 원인: 메뉴 `menuitemcheckbox` 선택 후 트리거 기억 누락 + React Strict Mode가 `takeRestoreTarget`을 두 번 호출해 pending이 비고, dialog 닫힌 뒤 `BODY`에 남음.

## Change

- `TestInstanceTable`: select-all `aria-label`/`title` = “Select all tests on this page”.
- `CommentComposer` + `ExecutionCommentsPanel`: `useId`/`htmlFor` + Test/Run discussion labels.
- `modalFocus`: `menuitemcheckbox` 스킵, `rememberModalRestoreTarget` + selector 재조회, Strict Mode용 `lastMenuTriggerSelector`, inert 해제 후 double-rAF/retry 복귀.
- `OverflowMenu`: 선택 시 `rememberModalRestoreTarget(triggerRef)`.

## Verification

- `npx vitest run src/shared/ui/modalFocus.test.ts src/features/runs/utils/runListStatusEntry.test.ts` (apps/web) — 7 passed.
- `npm.cmd run lint -w apps/web`, `npm.cmd run build -w apps/web` — pass.
- Live: `python docs/ux-evidence/_ui062-verify.py` → `ui-062-live-verify.json` **allOk true** (playwright-chromium-real-key-events).
- Screens: before 1280 run/discussion; after 1280 run/discussion/keyboard/closed; after 390 run.

## Exception

- 필수 결과 필드 미설정 fixture → validation alert 경로 N/A (`saved-no-required-fields-na`).
- Passed 필터 후 `selected` 카운트 UI가 빈 목록에서 숨겨짐(`selectedAfter=`). 행 숨김은 확인.
- ArrowDown 메뉴 이동은 OverflowMenu에 구현돼 있으나 이번 live 경로는 Failed `menuitemcheckbox`에 focus+Enter. Tab/Enter/Escape/복귀는 실키 검증.
- before 390 미촬영(결함은 1280 repro + after 390으로 입증).

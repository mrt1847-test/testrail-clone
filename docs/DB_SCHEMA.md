# DB Schema (Alias)

이 문서는 `DB_SCHEMA` 명칭을 사용하는 계획/체크리스트와의 정합성을 위해 유지하는 별칭 문서다.

- 기준 스키마 문서: [DATABASE_SCHEMA.md](./DATABASE_SCHEMA.md)
- 초기 구현 대상:
  - core tables
  - `test_results` metadata/error 필드
  - attachments 확장 자리

스키마 변경은 `DATABASE_SCHEMA.md`를 단일 소스로 수정하고, 이 문서는 참조 링크만 유지한다.

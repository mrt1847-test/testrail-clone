import { EmptyState } from "../../../shared/ui/EmptyState";

export function TokensPage() {
  return (
    <EmptyState
      title="API tokens"
      description="Token list and create/revoke dialogs will integrate with the tokens API (automation phase)."
    />
  );
}

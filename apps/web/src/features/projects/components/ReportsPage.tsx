import { EmptyState } from "../../../shared/ui/EmptyState";

export function ReportsPage() {
  return (
    <EmptyState
      title="Reports (1차 완성 예정)"
      description="Status distribution, failure trend, and run summary tables will load from aggregation APIs."
    />
  );
}
